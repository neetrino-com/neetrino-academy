# 🔒 Руководство по системе безопасности академии

## 📋 Обзор

Система безопасности академии обеспечивает централизованную защиту доступа на основе ролей пользователей. Реализована с использованием HOC (Higher Order Components) для автоматической проверки прав доступа.

## 🏗️ Архитектура безопасности

### Компоненты системы

1. **HOC `withRoleProtection`** - основной компонент защиты
2. **Специализированные HOC** - для конкретных ролей
3. **Защищенные layout'ы** - для разных типов пользователей
4. **Middleware** - серверная защита API
5. **Страница "Доступ запрещен"** - для пользователей без прав

## 🚀 Использование HOC

### Базовое использование

```typescript
import { withRoleProtection } from '@/components/auth/withRoleProtection'

function MyComponent({ userRole, isLoading }: WithRoleProtectionProps) {
  // Ваш компонент
}

// Защищаем компонент для администраторов и преподавателей
export default withRoleProtection(MyComponent, ['ADMIN', 'TEACHER'])
```

### Специализированные HOC

```typescript
import { 
  withAdminProtection, 
  withStaffProtection, 
  withStudentProtection 
} from '@/components/auth/withRoleProtection'

// Только для администраторов
export default withAdminProtection(AdminComponent)

// Для преподавателей и администраторов
export default withStaffProtection(TeacherComponent)

// Только для студентов
export default withStudentProtection(StudentComponent)
```

### Настройка опций

```typescript
export default withRoleProtection(MyComponent, ['ADMIN'], {
  redirectTo: '/custom-access-denied',    // Кастомная страница
  fallback: <CustomFallback />,           // Кастомный fallback
  loadingFallback: <CustomLoading />,     // Кастомная загрузка
  requireAuth: true,                      // Требовать авторизацию
  showAccessDenied: true                  // Показывать страницу доступа
})
```

## 🎯 Защита страниц

### Защита админских страниц

```typescript
// src/app/admin/page.tsx
import { withStaffProtection } from '@/components/auth/withRoleProtection'

function AdminDashboard({ userRole, isLoading }: WithRoleProtectionProps) {
  // Компонент дашборда
}

export default withStaffProtection(AdminDashboard)
```

### Защита страниц для студентов

```typescript
// src/app/dashboard/page.tsx
import { withStudentProtection } from '@/components/auth/withRoleProtection'

function StudentDashboard({ userRole, isLoading }: WithRoleProtectionProps) {
  // Компонент дашборда студента
}

export default withStudentProtection(StudentDashboard)
```

### Защита с множественными ролями

```typescript
// Доступ для администраторов, преподавателей и студентов
export default withRoleProtection(Component, ['ADMIN', 'TEACHER', 'STUDENT'])

// Доступ только для администраторов и преподавателей
export default withRoleProtection(Component, ['ADMIN', 'TEACHER'])
```

## 🎨 Использование защищенных layout'ов

### AdminLayout

```typescript
// src/app/admin/layout.tsx
import { AdminLayout } from '@/components/layouts/AdminLayout'

export default function AdminLayoutWrapper({
  children,
}: {
  children: React.ReactNode
}) {
  return <AdminLayout>{children}</AdminLayout>
}
```

### StudentLayout

```typescript
// src/app/dashboard/layout.tsx
import { StudentLayout } from '@/components/layouts/StudentLayout'

export default function DashboardLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <StudentLayout>{children}</StudentLayout>
}
```

## 🔧 API и параметры

### WithRoleProtectionProps

```typescript
interface WithRoleProtectionProps {
  userRole?: UserRole        // Роль пользователя
  isLoading?: boolean        // Статус загрузки
}
```

### ProtectionOptions

```typescript
interface ProtectionOptions {
  redirectTo?: string        // URL для перенаправления
  fallback?: ReactNode       // Fallback компонент
  loadingFallback?: ReactNode // Компонент загрузки
  requireAuth?: boolean      // Требовать авторизацию
  showAccessDenied?: boolean // Показывать страницу доступа
}
```

## 🧪 Тестирование безопасности

### Тестовая страница

Создана страница `/test-security` для тестирования системы безопасности:

- Доступна только для ADMIN и TEACHER
- Показывает информацию о роли пользователя
- Демонстрирует работу HOC

### Сценарии тестирования

1. **Вход с правами администратора** - должен получить доступ
2. **Вход с правами преподавателя** - должен получить доступ
3. **Вход с правами студента** - должен увидеть "Доступ запрещен"
4. **Вход без авторизации** - должен быть перенаправлен на логин

## 🚨 Обработка ошибок

### Страница "Доступ запрещен"

Автоматически показывается при отсутствии прав доступа:

- Объяснение причины отказа
- Кнопки навигации к доступным разделам
- Информация о получении доступа
- Красивый дизайн с градиентами

### Логирование

Все попытки доступа логируются в middleware:

- Успешные входы
- Попытки несанкционированного доступа
- Ошибки авторизации

## 📱 Адаптивность

### Мобильная версия

- Адаптивный дизайн для всех устройств
- Touch-friendly интерфейс
- Оптимизированные размеры кнопок

### Браузерная поддержка

- Современные браузеры (Chrome, Firefox, Safari, Edge)
- Поддержка ES6+ функций
- Graceful degradation для старых браузеров

## 🔐 Дополнительные меры безопасности

### Middleware

- Защита всех API маршрутов
- Проверка ролей на уровне сервера
- Rate limiting для предотвращения атак

### Валидация данных

- Проверка входных данных
- Санитизация пользовательского ввода
- Защита от XSS и CSRF атак

## 📚 Примеры использования

### Простая защита страницы

```typescript
// src/app/admin/users/page.tsx
import { withAdminProtection } from '@/components/auth/withRoleProtection'

function UsersPage({ userRole, isLoading }: WithRoleProtectionProps) {
  return (
    <div>
      <h1>Управление пользователями</h1>
      <p>Ваша роль: {userRole}</p>
    </div>
  )
}

export default withAdminProtection(UsersPage)
```

### Защита с кастомным fallback

```typescript
import { withRoleProtection } from '@/components/auth/withRoleProtection'

function CustomFallback() {
  return <div>У вас нет доступа к этому разделу</div>
}

export default withRoleProtection(Component, ['ADMIN'], {
  fallback: <CustomFallback />
})
```

### Защита с перенаправлением

```typescript
export default withRoleProtection(Component, ['ADMIN'], {
  redirectTo: '/dashboard',
  showAccessDenied: false
})
```

## 🎯 Лучшие практики

### 1. Всегда используйте HOC для защиты

```typescript
// ✅ Правильно
export default withStaffProtection(AdminComponent)

// ❌ Неправильно - нет защиты
export default AdminComponent
```

### 2. Используйте специализированные HOC

```typescript
// ✅ Лучше - более читаемо
export default withAdminProtection(Component)

// ❌ Хуже - менее читаемо
export default withRoleProtection(Component, ['ADMIN'])
```

### 3. Настраивайте fallback компоненты

```typescript
// ✅ Хорошо - кастомный fallback
export default withRoleProtection(Component, ['ADMIN'], {
  fallback: <CustomAccessDenied />
})

// ❌ Плохо - стандартный fallback может не подходить
export default withRoleProtection(Component, ['ADMIN'])
```

### 4. Тестируйте все роли

- Проверяйте доступ с каждой ролью
- Убедитесь, что fallback работает корректно
- Тестируйте перенаправления

## 🚀 Развертывание

### Продакшен

1. Удалите тестовую страницу `/test-security`
2. Проверьте все защищенные маршруты
3. Убедитесь в корректности middleware
4. Протестируйте с реальными пользователями

### Мониторинг

- Отслеживайте попытки несанкционированного доступа
- Мониторьте производительность HOC
- Логируйте ошибки безопасности

## 📞 Поддержка

При возникновении проблем с системой безопасности:

1. Проверьте консоль браузера на ошибки
2. Убедитесь в корректности ролей пользователей
3. Проверьте работу middleware
4. Обратитесь к документации NextAuth.js

---

**Система безопасности готова к использованию! 🎉**

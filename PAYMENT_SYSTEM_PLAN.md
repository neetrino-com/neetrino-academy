# 🎯 План реализации системы оплаты за курсы

## 📋 Обзор требований

Система должна поддерживать **2 типа оплаты**:

### 1. Разовая оплата (ONE_TIME)
- **Описание**: Оплата за весь курс независимо от длительности
- **Пример**: Курс стоит 30,000 ֏ и длится 5 месяцев. За весь период - 1 оплата 30,000 ֏
- **Логика**: При записи на курс создается один платеж на полную стоимость

### 2. Ежемесячная оплата (MONTHLY)
- **Описание**: Оплата каждый месяц за период обучения
- **Пример**: Курс стоит 30,000 ֏ и длится 5 месяцев. Каждый месяц нужно платить 30,000 ֏
- **Логика**: При записи на курс создается первый платеж, затем автоматически создаются следующие

## ✅ Что уже реализовано

### База данных
- ✅ Модель `Payment` с полями для типов оплаты
- ✅ Модель `Enrollment` с информацией о платежах
- ✅ Модель `Course` с полями `paymentType`, `monthlyPrice`, `totalPrice`
- ✅ Enum `PaymentType` (ONE_TIME, MONTHLY)
- ✅ Enum `PaymentStatus` (PENDING, PAID, OVERDUE, CANCELLED)

### API
- ✅ API записи на курс с автоматическим созданием платежей
- ✅ API для студенческих платежей
- ✅ API для административных платежей
- ✅ Функция создания следующих ежемесячных платежей

### Интерфейс
- ✅ Страница платежей для студентов (`/payments`)
- ✅ Страница управления платежами для админов (`/admin/payments`)
- ✅ Обновленная страница курса с информацией о типах оплаты
- ✅ Интеграция в админ-панель

## 🔧 Что нужно доработать

### 1. Система уведомлений о платежах ✅
- ✅ Уведомления о предстоящих платежах
- ✅ Уведомления о просрочке
- ✅ Уведомления об успешной оплате
- ✅ Автоматическое создание уведомлений при всех событиях
- [ ] Email уведомления (опционально)

### 2. Автоматическое приостановление доступа ✅
- ✅ Блокировка доступа к курсу при просрочке
- ✅ Автоматическое восстановление доступа после оплаты
- ✅ Уведомления о приостановлении
- ✅ API для управления доступом
- ✅ Компонент управления доступом в админ-панели

### 3. Интеграция с платежными системами
- [ ] Подключение реальных платежных шлюзов
- [ ] Обработка webhook'ов от платежных систем
- [ ] Безопасное хранение платежной информации

### 4. Финансовая отчетность
- [ ] Дашборд с общей статистикой по платежам
- [ ] Экспорт данных в Excel/CSV
- [ ] Аналитика по курсам и студентам

## 🚀 Алгоритм работы системы

### Запись на курс
1. Студент нажимает "Записаться на курс"
2. Система проверяет тип оплаты курса
3. Создается запись в `Enrollment` со статусом `PENDING`
4. Автоматически создается платеж в зависимости от типа:

#### Для ONE_TIME курса:
```typescript
await prisma.payment.create({
  data: {
    userId,
    courseId,
    amount: course.totalPrice,
    status: 'PENDING',
    paymentType: 'ONE_TIME',
    dueDate: new Date(Date.now() + 7 * 24 * 60 * 60 * 1000), // 7 дней
    notes: 'Разовая оплата за весь курс'
  }
})
```

#### Для MONTHLY курса:
```typescript
const firstPayment = await prisma.payment.create({
  data: {
    userId,
    courseId,
    amount: course.monthlyPrice,
    status: 'PENDING',
    paymentType: 'MONTHLY',
    monthNumber: 1,
    dueDate: new Date(Date.now() + 7 * 24 * 60 * 60 * 1000),
    notes: `Ежемесячный платеж 1/${course.duration}`
  }
})

// Обновляем запись на курс
await prisma.enrollment.update({
  where: { id: enrollment.id },
  data: { nextPaymentDue: firstPayment.dueDate }
})
```

### Оплата ежемесячного курса
1. Студент оплачивает текущий месяц
2. Система обновляет статус платежа на `PAID`
3. Автоматически создается следующий платеж (если курс не завершен)
4. Обновляется `nextPaymentDue` в записи на курс

```typescript
// Если это ежемесячный курс, создаем следующий платеж
if (payment.course.paymentType === 'MONTHLY' && payment.monthNumber) {
  const nextPayment = await createNextMonthlyPayment(
    user.id,
    payment.courseId,
    payment.monthNumber
  )
  
  if (nextPayment) {
    // Обновляем дату следующего платежа
    await prisma.enrollment.updateMany({
      where: { userId: user.id, courseId: payment.courseId },
      data: { nextPaymentDue: nextPayment.dueDate }
    })
  }
}
```

### Проверка доступа к курсу
1. Система проверяет статус записи на курс
2. Для разовой оплаты: проверяется наличие оплаченного платежа
3. Для ежемесячной оплаты: проверяется актуальность последнего оплаченного платежа
4. Доступ блокируется при неоплате или просрочке

## 🎨 UI/UX особенности

### Страница курса
- Отображение типа оплаты (Разовая/Ежемесячная)
- Показ стоимости: общая для разовой, ежемесячная + итоговая для месячной
- Индикатор длительности курса
- Кнопка записи с иконкой кредитной карты

### Страница платежей студента
- Статистика по всем платежам
- Список активных курсов с информацией о следующих платежах
- Предстоящие платежи с возможностью оплаты
- История платежей с детальной информацией

### Админ-панель платежей
- Общая статистика по статусам
- Фильтрация по статусу, типу оплаты, студенту, курсу
- Массовые операции (отметить как оплаченные, просроченные)
- Детальный просмотр каждого платежа

## 🔒 Безопасность

### Валидация
- Проверка ролей пользователей
- Валидация данных платежей
- Защита от дублирования платежей

### Аудит
- Логирование всех операций с платежами
- Отслеживание изменений статусов
- История действий администраторов

## 📊 Мониторинг и аналитика

### Метрики
- Общее количество платежей
- Процент успешных платежей
- Среднее время оплаты
- Популярность типов оплаты

### Уведомления
- Автоматические уведомления о статусах
- Алерты о критических ситуациях
- Отчеты для администраторов

## 🚀 Следующие шаги

### Приоритет 1 (Критично) ✅
1. ✅ Автоматическое создание платежей при записи на курс
2. ✅ Система проверки доступа по статусу оплаты
3. ✅ Интерфейс для управления платежами

### Приоритет 2 (Важно)
1. ✅ Система уведомлений о платежах
2. ✅ Автоматическое приостановление доступа
3. **Доработка курса билдера и редактора** 🔧
   - Добавить переключатель типов оплаты в курс билдер
   - Две красивые кнопки: "Разовая оплата" и "Ежемесячная оплата"
   - Красивый дизайн переключателя с анимацией
   - Сохранение выбранного типа в базе данных
   - Валидация полей в зависимости от типа оплаты
   - Обновление интерфейса при смене типа
4. **Обновление отображения курсов** 🎨
   - Добавить карточки с типом оплаты на странице `/courses`
   - Отображать "1 разовая оплата" или "X месяцев" на карточках курсов
   - Обновить страницу курса с детальной информацией об оплате
   - Интеграция с существующей системой платежей
5. Улучшенная финансовая отчетность

### Приоритет 3 (Желательно)
1. Интеграция с реальными платежными системами
2. Email уведомления
3. Расширенная аналитика

## 💡 Рекомендации по внедрению

### Тестирование
- Создать тестовые курсы с разными типами оплаты
- Протестировать все сценарии записи и оплаты
- Проверить корректность создания ежемесячных платежей

### Документация
- Обновить пользовательскую документацию
- Создать инструкции для администраторов
- Добавить FAQ по типам оплаты

### Обучение
- Провести обучение администраторов
- Создать видео-инструкции для студентов
- Подготовить ответы на частые вопросы

---

**Статус**: ✅ Система уведомлений и управления доступом реализована  
**Готовность**: 95%  
**Следующий этап**: Интеграция с реальными платежными системами

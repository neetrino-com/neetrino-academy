# 🎓 План системы управления курсами

## 🎯 Цель
Создать полноценный интерфейс для TEACHER и ADMIN ролей для управления курсами, модулями и уроками.

## 📋 Текущая ситуация

### ✅ Что уже работает:
- **Модели данных** - Course, Module, Lesson в базе данных
- **API маршруты** - базовые CRUD операции
- **Seed данные** - тестовые курсы создаются через seed файл
- **Роли пользователей** - TEACHER и ADMIN роли определены

### ❌ Что НЕ работает:
- **Интерфейс создания курсов** - нет форм для TEACHER/ADMIN
- **Интерфейс создания модулей** - нет форм для создания модулей
- **Интерфейс создания уроков** - нет форм для создания уроков
- **Защищенные маршруты** - нет проверки ролей для создания контента

## 🛠️ Что нужно создать

### 1. Защищенные маршруты
```
/admin/courses/          - Список всех курсов (ADMIN)
/admin/courses/create    - Создание нового курса (TEACHER/ADMIN)
/admin/courses/[id]      - Просмотр курса (TEACHER/ADMIN)
/admin/courses/[id]/edit - Редактирование курса (TEACHER/ADMIN)
/admin/courses/[id]/modules/create - Создание модуля (TEACHER/ADMIN)
/admin/modules/[id]/edit - Редактирование модуля (TEACHER/ADMIN)
/admin/modules/[id]/lessons/create - Создание урока (TEACHER/ADMIN)
/admin/lessons/[id]/edit - Редактирование урока (TEACHER/ADMIN)
```

### 2. Компоненты форм
```
src/components/admin/
├── CourseForm.tsx       - Форма создания/редактирования курса
├── ModuleForm.tsx       - Форма создания/редактирования модуля
├── LessonForm.tsx       - Форма создания/редактирования урока
├── CourseList.tsx       - Список курсов с действиями
├── ModuleList.tsx       - Список модулей курса
└── LessonList.tsx       - Список уроков модуля
```

### 3. API маршруты
```
src/app/api/admin/
├── courses/
│   ├── route.ts         - GET (список), POST (создание)
│   └── [id]/
│       ├── route.ts     - GET, PUT, DELETE
│       └── modules/
│           └── route.ts - POST (создание модуля)
├── modules/
│   └── [id]/
│       ├── route.ts     - GET, PUT, DELETE
│       └── lessons/
│           └── route.ts - POST (создание урока)
└── lessons/
    └── [id]/
        └── route.ts     - GET, PUT, DELETE
```

### 4. Middleware для проверки ролей
```typescript
// src/middleware/adminAuth.ts
export function adminAuth(handler: NextApiHandler) {
  return async (req: NextApiRequest, res: NextApiResponse) => {
    const session = await getServerSession(req, res, authOptions);
    
    if (!session || (session.user.role !== 'TEACHER' && session.user.role !== 'ADMIN')) {
      return res.status(403).json({ error: 'Доступ запрещен' });
    }
    
    return handler(req, res);
  };
}
```

## 📝 Детальный план реализации

### Этап 1: Базовые компоненты (1-2 дня)
1. **Создать CourseForm компонент**
   - Поля: title, description, direction, level, price, isActive
   - Валидация с Zod
   - Отправка на API

2. **Создать ModuleForm компонент**
   - Поля: title, description, order
   - Привязка к курсу
   - Валидация

3. **Создать LessonForm компонент**
   - Поля: title, content, videoUrl, duration, order
   - Привязка к модулю
   - Валидация

### Этап 2: API маршруты (1 день)
1. **Создать защищенные API маршруты**
   - POST /api/admin/courses - создание курса
   - PUT /api/admin/courses/[id] - редактирование курса
   - POST /api/admin/courses/[id]/modules - создание модуля
   - POST /api/admin/modules/[id]/lessons - создание урока

2. **Добавить проверку ролей**
   - Middleware для проверки TEACHER/ADMIN
   - Обработка ошибок доступа

### Этап 3: Страницы интерфейса (2-3 дня)
1. **Страница списка курсов** (`/admin/courses`)
   - Таблица всех курсов
   - Кнопки создания, редактирования, удаления
   - Фильтрация и поиск

2. **Страница создания курса** (`/admin/courses/create`)
   - Форма CourseForm
   - Валидация и обработка ошибок
   - Редирект после создания

3. **Страница редактирования курса** (`/admin/courses/[id]/edit`)
   - Загрузка данных курса
   - Форма CourseForm с предзаполненными данными
   - Обновление данных

4. **Страница управления модулями** (`/admin/courses/[id]/modules`)
   - Список модулей курса
   - Создание новых модулей
   - Редактирование существующих

5. **Страница управления уроками** (`/admin/modules/[id]/lessons`)
   - Список уроков модуля
   - Создание новых уроков
   - Редактирование существующих

### Этап 4: Навигация и UX (1 день)
1. **Добавить навигацию в админку**
   - Меню для TEACHER/ADMIN
   - Breadcrumbs для навигации
   - Кнопки быстрых действий

2. **Улучшить UX**
   - Подтверждения удаления
   - Уведомления об успешных операциях
   - Загрузочные состояния

### Этап 5: Тестирование и финальные штрихи (1 день)
1. **Тестирование функционала**
   - Проверка всех форм
   - Тестирование API маршрутов
   - Проверка прав доступа

2. **Документация**
   - Обновить PROGRESS.md
   - Создать инструкции для преподавателей
   - Обновить README

## 🎨 UI/UX требования

### Формы должны быть:
- **Интуитивными** - понятные поля и подписи
- **Валидируемыми** - проверка на клиенте и сервере
- **Отзывчивыми** - быстрая обратная связь
- **Доступными** - поддержка клавиатуры и скринридеров

### Навигация должна быть:
- **Логичной** - понятная иерархия
- **Быстрой** - минимум кликов
- **Контекстной** - breadcrumbs и заголовки

## 🔒 Безопасность

### Проверки ролей:
- **Все API маршруты** должны проверять роль пользователя
- **Все страницы** должны проверять роль пользователя
- **Middleware** для централизованной проверки

### Валидация данных:
- **Клиентская валидация** с Zod
- **Серверная валидация** с Zod
- **Санитизация** входных данных

## 📊 Метрики успеха

### Функциональные:
- ✅ TEACHER может создавать курсы
- ✅ TEACHER может создавать модули
- ✅ TEACHER может создавать уроки
- ✅ ADMIN имеет полный доступ
- ✅ STUDENT не имеет доступа к админке

### UX метрики:
- Время создания курса < 2 минуты
- Время создания модуля < 1 минута
- Время создания урока < 1 минута
- 0 ошибок валидации в продакшене

## 🚀 Следующие шаги

1. **Начать с CourseForm компонента**
2. **Создать базовые API маршруты**
3. **Добавить защищенные страницы**
4. **Протестировать функционал**
5. **Документировать изменения**

---

**Приоритет**: Высокий  
**Время реализации**: 5-7 дней  
**Статус**: Готов к началу разработки

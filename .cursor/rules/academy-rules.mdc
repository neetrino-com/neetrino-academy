# Правила Neetrino Academy

## 🔔 Обязательные уведомления

### Типы уведомлений:
- **🔴 Error** - критические ошибки (незаполненные поля, конфликты)
- **🟡 Warning** - предупреждения (короткие названия, рекомендации)
- **🟢 Success** - успешные действия
- **🔵 Info** - информация

### Примеры:
```typescript
// Ошибка валидации
{ type: 'error', title: 'Ошибка', message: 'Поле обязательно для заполнения', field: 'name' }

// Конфликт расписания
{ type: 'error', title: 'Конфликт', message: 'На это время уже есть занятие' }

// Успех
{ type: 'success', title: 'Готово', message: 'Данные сохранены' }
```

## ✅ Валидация

### Обязательные правила:
- **Названия:** минимум 3 символа
- **Email:** валидный формат
- **Пароль:** минимум 8 символов
- **Даты:** не в прошлом
- **Время:** формат HH:MM
- **Расписание:** проверка конфликтов

### Проверка конфликтов:
```typescript
const hasConflict = await prisma.groupSchedule.findFirst({
  where: {
    groupId,
    dayOfWeek,
    isActive: true,
    OR: [
      { startTime: { lte: startTime }, endTime: { gt: startTime } },
      { startTime: { lt: endTime }, endTime: { gte: endTime } }
    ]
  }
})
```

## 🎨 UI стандарты

### Цвета:
- Error: `#ef4444` (красный)
- Warning: `#f59e0b` (желтый)
- Success: `#10b981` (зеленый)
- Info: `#3b82f6` (синий)

### Позиции:
- Toast: правый верхний угол
- Inline: под полем
- Modal: центр экрана

## 📋 Чек-лист

### Перед разработкой:
- [ ] Определить обязательные поля
- [ ] Спланировать уведомления
- [ ] Продумать валидацию

### При разработке:
- [ ] Валидация на фронтенде
- [ ] Валидация на бэкенде
- [ ] Уведомления для всех сценариев
- [ ] Проверка конфликтов

## 🎯 Цель
Пользователи всегда понимают что происходит, ошибки предотвращаются, качество данных обеспечено.

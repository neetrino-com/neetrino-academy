# Правила разработки Neetrino Academy

## 🎯 Общие принципы

### 1. Пользовательский опыт (UX)
- Всегда думать о пользователе (учителя, студенты, администраторы)
- Интерфейс должен быть интуитивным и понятным
- Минимизировать количество кликов для выполнения основных действий
- Обеспечить быструю обратную связь для всех действий

### 2. Качество данных
- Всегда валидировать данные на фронтенде и бэкенде
- Предотвращать сохранение некорректных данных
- Обеспечивать целостность данных в базе

## 🔔 Система уведомлений

### Обязательные уведомления для создания:

#### 1. Валидация форм
```typescript
// Пример структуры уведомления
interface ValidationNotification {
  type: 'error' | 'warning' | 'success' | 'info'
  title: string
  message: string
  field?: string // поле, к которому относится ошибка
  action?: string // рекомендуемое действие
}
```

#### 2. Типы уведомлений по приоритету:

**🔴 Критические ошибки (error):**
- Обязательные поля не заполнены
- Неверный формат данных (email, телефон, дата)
- Превышение лимитов (количество студентов, время)
- Конфликты расписания
- Ошибки авторизации/доступа

**🟡 Предупреждения (warning):**
- Минимальное количество символов не достигнуто
- Рекомендуемые поля не заполнены
- Потенциальные конфликты
- Истекающие сроки

**🟢 Успешные действия (success):**
- Данные успешно сохранены
- Операция завершена
- Изменения применены

**🔵 Информационные (info):**
- Подсказки по использованию
- Статус операций
- Обновления системы

### 3. Правила валидации

#### Обязательные поля:
- **Название курса/группы:** минимум 3 символа, максимум 100
- **Email:** валидный формат email
- **Имя пользователя:** минимум 2 символа, максимум 50
- **Пароль:** минимум 8 символов, должен содержать буквы и цифры
- **Даты:** не может быть в прошлом для будущих событий
- **Время:** корректный формат HH:MM

#### Бизнес-логика:
- **Расписание:** проверка на конфликты времени
- **Группы:** максимальное количество студентов
- **Курсы:** уникальность названий
- **Платежи:** корректность сумм и валют

### 4. Примеры уведомлений

#### Создание расписания:
```typescript
// Конфликт времени
{
  type: 'error',
  title: 'Конфликт расписания',
  message: 'На это время уже запланировано занятие. Выберите другое время.',
  field: 'startTime'
}

// Недостаточно символов
{
  type: 'warning',
  title: 'Короткое название',
  message: 'Рекомендуется использовать минимум 5 символов для лучшего понимания.',
  field: 'title'
}

// Успешное создание
{
  type: 'success',
  title: 'Расписание создано',
  message: 'Новое занятие добавлено в расписание группы.'
}
```

#### Регистрация пользователя:
```typescript
// Неверный email
{
  type: 'error',
  title: 'Неверный формат email',
  message: 'Пожалуйста, введите корректный email адрес.',
  field: 'email'
}

// Слабый пароль
{
  type: 'warning',
  title: 'Слабый пароль',
  message: 'Рекомендуется использовать минимум 8 символов, включая буквы и цифры.',
  field: 'password'
}
```

## 📋 Правила разработки компонентов

### 1. Формы
- Всегда показывать валидацию в реальном времени
- Подсвечивать поля с ошибками
- Показывать количество символов для текстовых полей
- Блокировать отправку при наличии ошибок

### 2. Таблицы и списки
- Показывать количество элементов
- Информировать об отсутствии данных
- Предоставлять фильтры и поиск

### 3. Модальные окна
- Четкий заголовок и описание действия
- Кнопки с понятными названиями
- Возможность отмены действия

## 🎨 UI/UX стандарты

### Цветовая схема уведомлений:
- **🔴 Error:** `#ef4444` (красный)
- **🟡 Warning:** `#f59e0b` (желтый)
- **🟢 Success:** `#10b981` (зеленый)
- **🔵 Info:** `#3b82f6` (синий)

### Иконки:
- **Error:** ❌ или ⚠️
- **Warning:** ⚠️ или 💡
- **Success:** ✅ или 🎉
- **Info:** ℹ️ или 💡

### Позиционирование:
- **Toast уведомления:** правый верхний угол
- **Inline ошибки:** под полем ввода
- **Модальные уведомления:** центр экрана

## 🔧 Техническая реализация

### 1. Хук для уведомлений:
```typescript
const useNotifications = () => {
  const [notifications, setNotifications] = useState<Notification[]>([])
  
  const addNotification = (notification: Notification) => {
    setNotifications(prev => [...prev, notification])
    // Автоматическое удаление через 5 секунд
    setTimeout(() => removeNotification(notification.id), 5000)
  }
  
  return { notifications, addNotification, removeNotification }
}
```

### 2. Валидация форм:
```typescript
const validateForm = (data: FormData) => {
  const errors: ValidationError[] = []
  
  if (!data.title || data.title.length < 3) {
    errors.push({
      field: 'title',
      message: 'Название должно содержать минимум 3 символа'
    })
  }
  
  return errors
}
```

### 3. Проверка конфликтов:
```typescript
const checkScheduleConflict = async (groupId: string, dayOfWeek: number, startTime: string, endTime: string) => {
  const existingSchedule = await prisma.groupSchedule.findMany({
    where: {
      groupId,
      dayOfWeek,
      isActive: true,
      OR: [
        { startTime: { lte: startTime }, endTime: { gt: startTime } },
        { startTime: { lt: endTime }, endTime: { gte: endTime } }
      ]
    }
  })
  
  return existingSchedule.length > 0
}
```

## 📝 Чек-лист для разработки

### Перед созданием функционала:
- [ ] Определить все обязательные поля
- [ ] Установить правила валидации
- [ ] Спланировать уведомления для разных сценариев
- [ ] Продумать обработку ошибок

### При разработке:
- [ ] Реализовать валидацию на фронтенде
- [ ] Добавить валидацию на бэкенде
- [ ] Создать систему уведомлений
- [ ] Протестировать все сценарии

### После разработки:
- [ ] Проверить UX на разных устройствах
- [ ] Убедиться в понятности сообщений
- [ ] Протестировать граничные случаи
- [ ] Документировать правила валидации

## 🎯 Цель

Создать систему, где:
- Пользователи всегда понимают, что происходит
- Ошибки предотвращаются до сохранения
- Интерфейс помогает делать правильные действия
- Качество данных обеспечивается на всех уровнях
description:
globs:
alwaysApply: true
---

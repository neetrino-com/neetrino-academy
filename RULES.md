# 🚀 Правила разработки Academy Project

## 📋 Основные принципы

### 1. **Типизация TypeScript**
- ❌ **НЕ ИСПОЛЬЗОВАТЬ** тип `any`
- ✅ **ВСЕГДА** создавать конкретные интерфейсы и типы
- ✅ Использовать union types для ограниченных значений
- ✅ Типизировать все параметры функций и возвращаемые значения

**Пример правильной типизации:**
```typescript
// ❌ ПЛОХО
const updateData: any = {}

// ✅ ХОРОШО
const updateData: {
  name?: string;
  email?: string;
  role?: 'STUDENT' | 'TEACHER' | 'ADMIN';
  isActive?: boolean;
} = {}
```

### 2. **Переменные и Shadowing**
- ❌ **НЕ ИСПОЛЬЗОВАТЬ** имя переменной `module` (конфликт с Next.js)
- ✅ Использовать: `currentModule`, `existingModule`, `newModule`, `currentModuleData`
- ✅ Использовать `const` вместо `let` когда переменная не переназначается

**Пример:**
```typescript
// ❌ ПЛОХО - конфликт с Next.js
for (const module of modules) { ... }

// ✅ ХОРОШО
for (const currentModule of modules) { ... }
```

### 3. **Импорты и модули**
- ❌ **НЕ ИСПОЛЬЗОВАТЬ** `require()` 
- ✅ Использовать ES6 `import` или динамический `import()`
- ✅ Для Node.js модулей использовать динамический импорт

**Пример:**
```typescript
// ❌ ПЛОХО
const crypto = require('crypto');

// ✅ ХОРОШО
const crypto = await import('crypto');
```

### 4. **Обработка ошибок**
- ❌ **НЕ ИСПОЛЬЗОВАТЬ** `catch (error: any)`
- ✅ Использовать `catch (error: unknown)`
- ✅ Проверять тип ошибки перед использованием

**Пример:**
```typescript
// ❌ ПЛОХО
} catch (error: any) {
  console.error(error.message);
}

// ✅ ХОРОШО
} catch (error: unknown) {
  if (error instanceof Error) {
    console.error(error.message);
  }
}
```

## 🏗️ Структура проекта

### API Routes
- Все API endpoints должны быть типизированы
- Использовать Zod для валидации входных данных
- Возвращать типизированные ответы

### React Components
- Все пропсы должны быть типизированы
- Использовать `React.FC<Props>` или типизированные функции
- Избегать `any` в состояниях и пропсах

### Database (Prisma)
- Типизировать все `where` условия
- Создавать интерфейсы для сложных запросов
- Использовать типы Prisma для возвращаемых данных

## 🔧 Инструменты и линтеры

### ESLint правила
- `@typescript-eslint/no-explicit-any` - запрещает `any`
- `@next/next/no-assign-module-variable` - запрещает переменную `module`
- `@typescript-eslint/no-require-imports` - запрещает `require()`
- `prefer-const` - предпочитает `const` над `let`

### TypeScript конфигурация
- `strict: true` - строгий режим
- `noImplicitAny: true` - запрещает неявные `any`
- `strictNullChecks: true` - строгие проверки null

## 📝 Примеры хорошего кода

### Интерфейсы для API
```typescript
interface CreateUserRequest {
  name: string;
  email: string;
  role: 'STUDENT' | 'TEACHER' | 'ADMIN';
  password: string;
}

interface UserResponse {
  id: string;
  name: string;
  email: string;
  role: string;
  createdAt: Date;
}
```

### Типизированные функции
```typescript
async function createUser(data: CreateUserRequest): Promise<UserResponse> {
  // реализация
}

function filterUsers(users: UserResponse[], role: string): UserResponse[] {
  return users.filter(user => user.role === role);
}
```

### Prisma запросы
```typescript
const whereCondition: {
  role?: 'STUDENT' | 'TEACHER' | 'ADMIN';
  isActive?: boolean;
  OR?: Array<{
    name?: { contains: string; mode: 'insensitive' };
    email?: { contains: string; mode: 'insensitive' };
  }>;
} = {
  role: 'STUDENT',
  isActive: true
}
```

## 🚨 Что НЕ делать

1. **НЕ используйте `any`** - всегда создавайте конкретные типы
2. **НЕ называйте переменные `module`** - используйте альтернативные имена
3. **НЕ используйте `require()`** - используйте ES6 импорты
4. **НЕ игнорируйте ошибки TypeScript** - исправляйте их сразу
5. **НЕ создавайте пустые интерфейсы** - всегда добавляйте свойства

## 📚 Полезные ресурсы

- [TypeScript Handbook](https://www.typescriptlang.org/docs/)
- [Prisma Documentation](https://www.prisma.io/docs/)
- [Next.js Documentation](https://nextjs.org/docs)
- [ESLint Rules](https://eslint.org/docs/rules/)

## 🔍 Проверка перед коммитом

Перед каждым коммитом убедитесь, что:
1. ✅ `npm run build` проходит без ошибок
2. ✅ `npm run lint` не показывает критических ошибок
3. ✅ Все новые функции типизированы
4. ✅ Нет использования `any` типов
5. ✅ Нет переменных с именем `module`

---

**Помните: Хорошая типизация = Лучший код = Меньше багов = Счастливые пользователи! 🎯**

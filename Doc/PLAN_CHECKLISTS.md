# 📋 План разработки системы чеклистов для академии

## 🎯 Обзор задачи

Добавить новую функциональность в админ-панель - **"Управление чеклистами"**, которая позволит:

1. **Создавать и управлять чеклистами** аналогично управлению курсами
2. **Группировать чеклисты по направлениям** (WordPress, Vibe Coding, Shopify)
3. **Использовать чеклисты в уроках** как лекции
4. **Отслеживать прогресс студентов** по выполнению пунктов

## 🏗️ Архитектура чеклистов

### Структура чеклиста

```
📋 Чеклист: "Настройка WordPress"
├── 📁 Группа 1: "Подготовка сервера" (можно скрыть/показать)
│   ├── ✅ Пункт 1.1: Выбрать хостинг
│   ├── ❌ Пункт 1.2: Настроить домен
│   ├── ❓ Пункт 1.3: Установить SSL
│   └── 🚫 Пункт 1.4: Настроить CDN (не нужно)
├── 📁 Группа 2: "Установка CMS"
│   ├── ✅ Пункт 2.1: Скачать WordPress
│   ├── ❌ Пункт 2.2: Настроить базу данных
│   └── ❓ Пункт 2.3: Запустить установку (есть вопросы)
└── 📁 Группа 3: "Первые настройки"
    ├── ❌ Пункт 3.1: Выбрать тему
    └── ❌ Пункт 3.2: Установить плагины
```

### Статусы выполнения

- ✅ **Выполнено** - пункт полностью завершен
- ❌ **Не выполнено** - пункт еще не начат
- 🚫 **Не нужно** - пункт неактуален для данного студента
- ❓ **Есть вопросы** - нужна помощь преподавателя

## 🗄️ Модели базы данных

### 1. Модель Checklist

```prisma
model Checklist {
  id          String   @id @default(cuid())
  title       String
  description String?
  direction   Direction // WORDPRESS, VIBE_CODING, SHOPIFY
  thumbnail   String?
  isActive    Boolean  @default(true)
  createdBy   String
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  
  creator     User              @relation("ChecklistCreator", fields: [createdBy], references: [id])
  groups      ChecklistGroup[]
  lessons     Lesson[]          // связь с уроками
  progress    ChecklistProgress[]
}
```

### 2. Модель ChecklistGroup (Группы действий)

```prisma
model ChecklistGroup {
  id          String   @id @default(cuid())
  title       String
  description String?
  order       Int
  isCollapsed Boolean  @default(false) // скрыта ли группа по умолчанию
  checklistId String
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  
  checklist   Checklist        @relation(fields: [checklistId], references: [id], onDelete: Cascade)
  items       ChecklistItem[]
}
```

### 3. Модель ChecklistItem (Пункты чеклиста)

```prisma
model ChecklistItem {
  id          String   @id @default(cuid())
  title       String
  description String?
  order       Int
  isRequired  Boolean  @default(true) // обязательный ли пункт
  groupId     String
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  
  group       ChecklistGroup    @relation(fields: [groupId], references: [id], onDelete: Cascade)
  progress    ChecklistItemProgress[]
}
```

### 4. Модель ChecklistProgress (Общий прогресс по чеклисту)

```prisma
model ChecklistProgress {
  id          String   @id @default(cuid())
  userId      String
  checklistId String
  startedAt   DateTime @default(now())
  completedAt DateTime?
  progress    Float    @default(0) // процент выполнения 0-100
  
  user        User              @relation(fields: [userId], references: [id])
  checklist   Checklist         @relation(fields: [checklistId], references: [id])
  
  @@unique([userId, checklistId])
}
```

### 5. Модель ChecklistItemProgress (Прогресс по пунктам)

```prisma
model ChecklistItemProgress {
  id        String              @id @default(cuid())
  userId    String
  itemId    String
  status    ChecklistItemStatus @default(NOT_COMPLETED)
  comment   String?             // комментарий студента
  updatedAt DateTime            @updatedAt
  
  user      User                @relation(fields: [userId], references: [id])
  item      ChecklistItem       @relation(fields: [itemId], references: [id])
  
  @@unique([userId, itemId])
}

enum ChecklistItemStatus {
  COMPLETED     // ✅ Выполнено
  NOT_COMPLETED // ❌ Не выполнено  
  NOT_NEEDED    // 🚫 Не нужно
  HAS_QUESTIONS // ❓ Есть вопросы
}
```

### 6. Обновление модели Lesson

```prisma
model Lesson {
  // ... существующие поля
  checklistId String?          // Связь с чеклистом
  checklist   Checklist?       @relation(fields: [checklistId], references: [id])
}
```

## 📋 Этапы разработки

### Этап 1: Подготовка базы данных (1-2 часа)

**Задачи:**
- [ ] Создать миграцию с новыми моделями чеклистов
- [ ] Обновить схему Prisma
- [ ] Добавить связь checklistId в модель Lesson
- [ ] Запустить миграцию и обновить клиент Prisma

**Файлы для создания/изменения:**
- `prisma/schema.prisma` - добавить новые модели
- `prisma/migrations/` - новая миграция

### Этап 2: API для чеклистов (2-3 часа)

**Задачи:**
- [ ] Создать CRUD API для чеклистов `/api/admin/checklists`
- [ ] Создать API для групп `/api/admin/checklists/[id]/groups`
- [ ] Создать API для пунктов `/api/admin/checklists/groups/[id]/items`
- [ ] Создать API для прогресса студентов `/api/student/checklists/[id]/progress`

**Файлы для создания:**
- `src/app/api/admin/checklists/route.ts` - список чеклистов (GET, POST)
- `src/app/api/admin/checklists/[id]/route.ts` - операции с чеклистом (GET, PUT, DELETE)
- `src/app/api/admin/checklists/[id]/groups/route.ts` - группы чеклиста
- `src/app/api/admin/checklists/groups/[groupId]/items/route.ts` - пункты группы
- `src/app/api/student/checklists/[id]/route.ts` - просмотр чеклиста студентом
- `src/app/api/student/checklists/[id]/progress/route.ts` - обновление прогресса

### Этап 3: Админ-панель управления чеклистами (3-4 часа)

**Задачи:**
- [ ] Добавить блок "Чеклисты" в главную админ-панель
- [ ] Создать страницу списка чеклистов `/admin/checklists`
- [ ] Создать страницу создания чеклиста `/admin/checklists/create`
- [ ] Создать страницу редактирования `/admin/checklists/[id]/edit`
- [ ] Создать страницу просмотра `/admin/checklists/[id]`

**Файлы для создания:**
- `src/app/admin/checklists/page.tsx` - список чеклистов
- `src/app/admin/checklists/create/page.tsx` - создание чеклиста
- `src/app/admin/checklists/[id]/page.tsx` - просмотр чеклиста
- `src/app/admin/checklists/[id]/edit/page.tsx` - редактирование

**Обновить:**
- `src/app/admin/page.tsx` - добавить блок "Чеклисты"

### Этап 4: Компоненты для работы с чеклистами (4-5 часов)

**Задачи:**
- [ ] Создать форму создания/редактирования чеклиста
- [ ] Создать компонент управления группами
- [ ] Создать компонент управления пунктами
- [ ] Создать компонент просмотра прогресса студентов

**Файлы для создания:**
- `src/components/admin/ChecklistForm.tsx` - форма чеклиста
- `src/components/admin/ChecklistGroupManager.tsx` - управление группами
- `src/components/admin/ChecklistItemManager.tsx` - управление пунктами
- `src/components/admin/ChecklistProgress.tsx` - прогресс студентов

### Этап 5: Интеграция с уроками (2-3 часа)

**Задачи:**
- [ ] Обновить конструктор курсов для выбора чеклистов
- [ ] Добавить возможность привязки чеклиста к уроку
- [ ] Обновить API уроков для поддержки checklistId

**Файлы для изменения:**
- `src/app/admin/builder/page.tsx` - добавить выбор чеклиста
- `src/components/admin/LessonForm.tsx` - добавить поле чеклиста
- `src/app/api/admin/lessons/[id]/route.ts` - поддержка checklistId

### Этап 6: Интерфейс для студентов (3-4 часа)

**Задачи:**
- [ ] Создать страницу чеклиста для студентов
- [ ] Реализовать интерактивные группы (сворачивание/разворачивание)
- [ ] Добавить возможность изменения статусов пунктов
- [ ] Интегрировать чеклисты в страницы уроков

**Файлы для создания:**
- `src/app/checklists/[id]/page.tsx` - страница чеклиста для студента
- `src/components/student/ChecklistViewer.tsx` - просмотр чеклиста
- `src/components/student/ChecklistItem.tsx` - пункт чеклиста
- `src/components/student/ChecklistGroup.tsx` - группа пунктов

**Обновить:**
- `src/app/courses/[id]/lessons/[lessonId]/page.tsx` - добавить чеклист в урок

### Этап 7: Система прогресса и аналитика (2-3 часа)

**Задачи:**
- [ ] Реализовать подсчет прогресса по чеклистам
- [ ] Добавить статистику в админ-панель
- [ ] Создать уведомления о завершении чеклистов
- [ ] Интегрировать прогресс в общую аналитику курса

**Файлы для изменения:**
- `src/app/api/analytics/route.ts` - добавить статистику чеклистов
- `src/lib/notifications.ts` - уведомления о чеклистах
- `src/components/analytics/ChecklistStats.tsx` - статистика чеклистов

## 🎨 Дизайн и UX

### Принципы дизайна

1. **Консистентность** - использовать тот же стиль, что и для лекций
2. **Интуитивность** - четкие иконки для статусов выполнения
3. **Группировка** - возможность сворачивать/разворачивать группы
4. **Прогресс** - визуальные индикаторы выполнения

### Цветовая схема для чеклистов

- **Основной цвет**: `amber-500` (янтарный) - для выделения среди других элементов
- **Выполнено**: `green-500` - ✅ зеленый
- **Не выполнено**: `gray-400` - ❌ серый
- **Не нужно**: `red-500` - 🚫 красный
- **Есть вопросы**: `orange-500` - ❓ оранжевый

### Иконки

- 📋 `ClipboardCheck` - основная иконка чеклистов
- 📁 `Folder` - группы
- ✅ `CheckCircle` - выполнено
- ❌ `Circle` - не выполнено
- 🚫 `XCircle` - не нужно
- ❓ `HelpCircle` - есть вопросы

## 🔧 Технические решения

### 1. Хранение структуры чеклиста

**Преимущества реляционного подхода** (выбранный):
- Гибкие запросы для аналитики
- Простая работа с правами доступа
- Легкое изменение структуры

### 2. Система прогресса

**Двухуровневая система**:
- `ChecklistProgress` - общий прогресс по чеклисту
- `ChecklistItemProgress` - прогресс по каждому пункту

### 3. Интеграция с уроками

**Связь через checklistId**:
- Один чеклист может использоваться в нескольких уроках
- У урока может быть одновременно лекция, задание, тест И чеклист

## 📊 Примеры использования

### Для WordPress направления

**Чеклист: "Создание первого сайта"**
- Группа 1: Подготовка (выбор хостинга, домена)
- Группа 2: Установка WordPress
- Группа 3: Базовые настройки
- Группа 4: Выбор темы
- Группа 5: Основные плагины

### Для Vibe Coding направления

**Чеклист: "Настройка среды разработки"**
- Группа 1: Установка инструментов (VS Code, Git, Node.js)
- Группа 2: Настройка проекта
- Группа 3: Первый код
- Группа 4: Тестирование
- Группа 5: Деплой

### Для Shopify направления

**Чеклист: "Запуск интернет-магазина"**
- Группа 1: Регистрация Shopify
- Группа 2: Настройка магазина
- Группа 3: Добавление товаров
- Группа 4: Настройка оплаты
- Группа 5: Тестовые заказы

## ⏱️ Временные рамки

**Общее время разработки: 15-20 часов**

| Этап | Время | Описание |
|------|-------|----------|
| 1 | 1-2 ч | База данных и миграции |
| 2 | 2-3 ч | API роуты |
| 3 | 3-4 ч | Админ-панель |
| 4 | 4-5 ч | Компоненты админки |
| 5 | 2-3 ч | Интеграция с уроками |
| 6 | 3-4 ч | Интерфейс студентов |
| 7 | 2-3 ч | Прогресс и аналитика |

## 🎯 Критерии успеха

### Функциональные

- [ ] Администратор может создавать и редактировать чеклисты
- [ ] Чеклисты можно привязывать к урокам
- [ ] Студенты могут отмечать выполнение пунктов
- [ ] Группы можно сворачивать/разворачивать
- [ ] Доступны все 4 статуса выполнения
- [ ] Прогресс отслеживается и отображается

### Технические

- [ ] API соответствует REST принципам
- [ ] Код покрыт TypeScript типами
- [ ] Компоненты переиспользуемы
- [ ] Производительность не ухудшилась
- [ ] Мобильная версия работает корректно

### UX

- [ ] Интерфейс интуитивно понятен
- [ ] Дизайн консистентен с остальной системой
- [ ] Быстрая работа без лагов
- [ ] Четкая визуализация прогресса

## 📋 Следующие шаги

1. **Подтвердить план** с пользователем
2. **Выбрать приоритет** - какие этапы делать в первую очередь
3. **Начать разработку** с базы данных и API
4. **Итеративная разработка** - тестировать каждый этап

**Готовы начинать разработку системы чеклистов! 🚀**

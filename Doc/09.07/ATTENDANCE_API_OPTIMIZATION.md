# Оптимизация API для журнала посещаемости

## Проблема
В компоненте `AttendanceJournal` использовались **2 разных API endpoint'а**:
1. `/api/admin/groups/${groupId}/attendance` - для обычного режима (таблица/карточки)
2. `/api/admin/groups/${groupId}/attendance/monthly` - для календарного режима

Это приводило к:
- Дублированию кода
- Неэффективному использованию ресурсов
- Сложности в поддержке

## Решение
Создан **единый универсальный API endpoint** `/api/admin/groups/${groupId}/attendance` с поддержкой параметров:

### Параметры запроса
- `view` - режим отображения (`table` | `calendar`)
- `year` - год для календарного режима
- `month` - месяц для календарного режима

### Примеры использования

#### Обычный режим (таблица/карточки)
```javascript
fetch('/api/admin/groups/group3/attendance?view=table')
```

#### Календарный режим
```javascript
fetch('/api/admin/groups/group3/attendance?view=calendar&year=2024&month=12')
```

## Изменения в API

### GET метод
- Добавлена поддержка параметра `view` для выбора режима
- Добавлена поддержка параметров `year` и `month` для календарного режима
- Логика получения данных адаптирована под разные режимы
- Возвращаются дополнительные поля для календарного режима

### PATCH метод
- Добавлена поддержка параметра `date` для календарного режима
- Логика поиска события адаптирована под разные режимы

## Преимущества оптимизации

1. **Единый endpoint** - упрощает использование и поддержку
2. **Меньше дублирования кода** - общая логика в одном месте
3. **Лучшая производительность** - один запрос вместо двух
4. **Простота тестирования** - один API для всех режимов
5. **Консистентность данных** - одинаковые данные для всех режимов

## Следующие шаги

1. ✅ Обновлен основной API endpoint
2. 🔄 Обновить компонент AttendanceJournal (в процессе)
3. ⏳ Удалить старый monthly API endpoint
4. ⏳ Протестировать все режимы отображения

## Файлы для удаления
После завершения миграции можно удалить:
- `src/app/api/admin/groups/[id]/attendance/monthly/route.ts`

## Тестирование
Создан тестовый скрипт `test-unified-api.js` для проверки работы API.


# Обновление: Редактирование даты и времени событий

## ✅ Что добавлено

### 1. Поля для редактирования даты и времени
В модальном окне "Редактировать событие" используются поля:

- **Дата события** - выбор даты события (одна дата)
- **Время начала** - выбор времени начала события  
- **Время окончания** - выбор времени окончания события

> **Важно**: В системе занятий каждое событие имеет одну дату и два времени (начало и конец), а не две отдельные даты.

### 2. Валидация данных
Добавлена проверка корректности введенных данных:

- Проверка заполненности всех обязательных полей
- Проверка, что время окончания после времени начала
- Проверка корректности названия события

### 3. Автоматическое заполнение полей
При открытии модального окна редактирования:

- Поля даты и времени автоматически заполняются текущими значениями события
- Дата конвертируется в формат YYYY-MM-DD для input[type="date"]
- Время конвертируется в формат HH:MM для input[type="time"]

## 🎯 Как использовать

### Редактирование даты и времени события
1. В списке событий нажмите кнопку "Редактировать" (карандаш)
2. В модальном окне измените нужные поля:
   - **Дата события** - выберите дату события
   - **Время начала** - выберите время начала
   - **Время окончания** - выберите время окончания
3. Нажмите "Сохранить изменения"

### Валидация
Система автоматически проверит:
- Все поля даты и времени заполнены
- Время окончания после времени начала
- Название события не пустое

## 🔧 Технические детали

### Файлы изменений
- `src/app/admin/groups/[id]/schedule/page.tsx` - основная логика редактирования

### Поля в состоянии
```typescript
const [editFormData, setEditFormData] = useState({
  title: '',
  description: '',
  type: EVENT_TYPES.LESSON,
  location: '',
  eventDate: '',      // Одна дата для события
  startTime: '',      // Время начала
  endTime: '',        // Время окончания
  isAttendanceRequired: false
})
```

### Формат данных
- **Дата**: YYYY-MM-DD (совместимо с input[type="date"])
- **Время**: HH:MM (совместимо с input[type="time"])
- **API**: ISO 8601 формат для передачи на сервер

## 🚀 Результат

Теперь в модальном окне "Редактировать событие" можно:
- ✅ Изменять дату события
- ✅ Изменять время начала события
- ✅ Изменять время окончания события
- ✅ Валидация корректности данных
- ✅ Автоматическое заполнение текущих значений
- ✅ Логика соответствует системе занятий (1 дата + 2 времени)

Система полностью поддерживает редактирование всех аспектов события включая дату и время! 🎉

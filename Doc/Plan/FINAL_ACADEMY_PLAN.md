# 🚀 Neetrino Academy - Финальный план разработки

## 📋 Обзор проекта
Веб-приложение для IT-компании Neetrino с тремя направлениями обучения:
1. **WordPress** (1 этап) - MVP
2. **Vibe Coding** (2 этап) - Web Development
3. **Shopify** (3 этап) - E-commerce

---

## 🛠️ Технологический стек

### Frontend
- **Next.js 14** (App Router) - React фреймворк
- **TypeScript** - типизация
- **Tailwind CSS** - стилизация
- **Shadcn/ui** - компоненты
- **Framer Motion** - анимации
- **React Hook Form** - формы
- **Zustand** - состояние

### Backend
- **Next.js API Routes** - API endpoints
- **Prisma** - ORM
- **PostgreSQL** - база данных
- **NextAuth.js** - аутентификация
- **Zod** - валидация

### Инфраструктура
- **Vercel** - хостинг
- **Supabase** - база данных
- **Cloudinary** - медиа файлы
- **Stripe** - платежи
- **Resend** - email

---

## 📁 Структура проекта

```
academy-web/
├── src/
│   ├── app/                    # Next.js App Router
│   │   ├── (auth)/            # Группа роутов аутентификации
│   │   │   ├── login/
│   │   │   ├── register/
│   │   │   └── forgot-password/
│   │   ├── (dashboard)/       # Группа роутов дашборда
│   │   │   ├── dashboard/
│   │   │   ├── courses/
│   │   │   ├── lessons/
│   │   │   ├── assignments/
│   │   │   ├── profile/
│   │   │   └── settings/
│   │   ├── (admin)/           # Группа роутов админки
│   │   │   ├── admin/
│   │   │   ├── users/
│   │   │   ├── courses/
│   │   │   └── analytics/
│   │   ├── api/               # API endpoints
│   │   │   ├── auth/
│   │   │   ├── courses/
│   │   │   ├── lessons/
│   │   │   ├── assignments/
│   │   │   └── users/
│   │   ├── globals.css
│   │   ├── layout.tsx
│   │   └── page.tsx
│   ├── components/            # Переиспользуемые компоненты
│   │   ├── ui/               # UI компоненты (shadcn)
│   │   ├── forms/            # Формы
│   │   ├── layout/           # Компоненты макета
│   │   ├── dashboard/        # Компоненты дашборда
│   │   ├── courses/          # Компоненты курсов
│   │   └── admin/            # Компоненты админки
│   ├── lib/                  # Утилиты и конфигурация
│   │   ├── auth.ts
│   │   ├── db.ts
│   │   ├── utils.ts
│   │   └── validations.ts
│   ├── hooks/                # Кастомные хуки
│   ├── stores/               # Zustand stores
│   └── types/                # TypeScript типы
├── prisma/                   # Схема базы данных
│   └── schema.prisma
├── public/                   # Статические файлы
├── .env.example
├── package.json
└── README.md
```

---

## 🗄️ Схема базы данных

```prisma
// Пользователи
model User {
  id            String   @id @default(cuid())
  email         String   @unique
  name          String?
  role          UserRole @default(STUDENT)
  avatar        String?
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
  
  // Связи
  enrollments   Enrollment[]
  assignments   Assignment[]
  submissions   Submission[]
  messages      Message[]
  achievements  Achievement[]
}

// Курсы
model Course {
  id          String   @id @default(cuid())
  title       String
  description String?
  slug        String   @unique
  direction   Direction
  level       Level
  price       Decimal?
  isActive    Boolean  @default(true)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  
  // Связи
  modules     Module[]
  enrollments Enrollment[]
}

// Модули
model Module {
  id          String   @id @default(cuid())
  title       String
  description String?
  order       Int
  courseId    String
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  
  // Связи
  course      Course     @relation(fields: [courseId], references: [id])
  lessons     Lesson[]
  assignments Assignment[]
}

// Уроки
model Lesson {
  id          String   @id @default(cuid())
  title       String
  content     String?
  videoUrl    String?
  duration    Int?     // в минутах
  order       Int
  moduleId    String
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  
  // Связи
  module      Module @relation(fields: [moduleId], references: [id])
  progress    LessonProgress[]
}

// Задания
model Assignment {
  id          String   @id @default(cuid())
  title       String
  description String?
  dueDate     DateTime?
  moduleId    String
  createdBy   String
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  
  // Связи
  module      Module      @relation(fields: [moduleId], references: [id])
  creator     User        @relation(fields: [createdBy], references: [id])
  submissions Submission[]
}

// Прогресс уроков
model LessonProgress {
  id        String   @id @default(cuid())
  userId    String
  lessonId  String
  completed Boolean  @default(false)
  progress  Float    @default(0) // 0-100%
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  
  // Связи
  user      User   @relation(fields: [userId], references: [id])
  lesson    Lesson @relation(fields: [lessonId], references: [id])
  
  @@unique([userId, lessonId])
}

// Записи на курсы
model Enrollment {
  id        String   @id @default(cuid())
  userId    String
  courseId  String
  status    EnrollmentStatus @default(ACTIVE)
  enrolledAt DateTime @default(now())
  
  // Связи
  user      User   @relation(fields: [userId], references: [id])
  course    Course @relation(fields: [courseId], references: [id])
  
  @@unique([userId, courseId])
}

// Сдачи заданий
model Submission {
  id           String   @id @default(cuid())
  userId       String
  assignmentId String
  content      String?
  fileUrl      String?
  score        Float?
  feedback     String?
  submittedAt  DateTime @default(now())
  gradedAt     DateTime?
  
  // Связи
  user         User       @relation(fields: [userId], references: [id])
  assignment   Assignment @relation(fields: [assignmentId], references: [id])
}

// Сообщения
model Message {
  id        String   @id @default(cuid())
  content   String
  userId    String
  courseId  String?
  createdAt DateTime @default(now())
  
  // Связи
  user      User   @relation(fields: [userId], references: [id])
  course    Course? @relation(fields: [courseId], references: [id])
}

// Достижения
model Achievement {
  id          String   @id @default(cuid())
  userId      String
  type        AchievementType
  title       String
  description String?
  earnedAt    DateTime @default(now())
  
  // Связи
  user        User @relation(fields: [userId], references: [id])
}

// Enums
enum UserRole {
  STUDENT
  TEACHER
  ADMIN
}

enum Direction {
  WORDPRESS
  VIBE_CODING
  SHOPIFY
}

enum Level {
  BEGINNER
  INTERMEDIATE
  ADVANCED
}

enum EnrollmentStatus {
  ACTIVE
  COMPLETED
  CANCELLED
}

enum AchievementType {
  COURSE_COMPLETED
  ASSIGNMENT_SUBMITTED
  PERFECT_SCORE
  STREAK_7_DAYS
  STREAK_30_DAYS
}
```

---

## 🎯 Пошаговый план реализации

### ФАЗА 1: MVP (2-3 недели) - WordPress направление

#### Неделя 1: Основа проекта
**День 1-2: Настройка проекта**
- [ ] Инициализация Next.js проекта
- [ ] Настройка TypeScript, Tailwind CSS, Shadcn/ui
- [ ] Настройка Prisma и PostgreSQL
- [ ] Базовая структура папок

**День 3-4: Аутентификация**
- [ ] Настройка NextAuth.js
- [ ] Создание схемы пользователей
- [ ] Страницы login/register
- [ ] Middleware для защиты роутов

**День 5-7: Базовый дашборд**
- [ ] Layout с навигацией
- [ ] Главная страница дашборда
- [ ] Профиль пользователя
- [ ] Настройки аккаунта

#### Неделя 2: Система курсов
**День 1-3: Управление курсами**
- [ ] CRUD операции для курсов
- [ ] Страница списка курсов
- [ ] Детальная страница курса
- [ ] Система записи на курсы

**День 4-5: Модули и уроки**
- [ ] Структура модулей
- [ ] Видео-плеер для уроков
- [ ] Прогресс обучения
- [ ] Навигация по урокам

**День 6-7: Задания**
- [ ] Создание заданий
- [ ] Система сдачи работ
- [ ] Проверка заданий
- [ ] Обратная связь

#### Неделя 3: Финальные штрихи MVP
**День 1-3: Админка**
- [ ] Панель администратора
- [ ] Управление пользователями
- [ ] Управление курсами
- [ ] Базовая аналитика

**День 4-5: WordPress специфика**
- [ ] Песочница WordPress
- [ ] Шаблоны заданий
- [ ] Автоматические проверки
- [ ] Портфолио проектов

**День 6-7: Тестирование и деплой**
- [ ] Тестирование функционала
- [ ] Исправление багов
- [ ] Деплой на Vercel
- [ ] Настройка домена

### ФАЗА 2: Расширение (3-4 недели)

#### Неделя 4-5: Vibe Coding направление
- [ ] Добавление направления в схему
- [ ] Специфичные задания для веб-разработки
- [ ] Интегрированный редактор кода
- [ ] Система код-ревью
- [ ] GitHub интеграция

#### Неделя 6-7: Shopify направление
- [ ] Добавление Shopify направления
- [ ] Тестовые магазины
- [ ] Liquid шаблоны
- [ ] E-commerce задания

### ФАЗА 3: Продвинутые функции (4-5 недель)

#### Неделя 8-9: Гамификация
- [ ] Система достижений
- [ ] Рейтинги и лидерборды
- [ ] XP и уровни
- [ ] Бейджи и награды

#### Неделя 10-11: Сообщество
- [ ] Форумы и обсуждения
- [ ] Система менторства
- [ ] Групповые проекты
- [ ] Чат между участниками

#### Неделя 12: Аналитика и оптимизация
- [ ] Продвинутая аналитика
- [ ] Отчеты и дашборды
- [ ] Оптимизация производительности
- [ ] Мобильная версия

---

## 🎨 UI/UX концепция

### Дизайн система
- **Цветовая палитра**: Современная, профессиональная
- **Типографика**: Читаемая, иерархичная
- **Компоненты**: Модульные, переиспользуемые
- **Анимации**: Плавные, ненавязчивые

### Ключевые экраны
1. **Лендинг** - привлекательная главная страница
2. **Дашборд** - персональная панель студента
3. **Курсы** - каталог и детали курсов
4. **Уроки** - интерактивное обучение
5. **Задания** - система сдачи работ
6. **Профиль** - личный кабинет
7. **Админка** - управление платформой

---

## 🔧 Технические особенности

### Производительность
- **SSR/SSG** для быстрой загрузки
- **Image optimization** для медиа
- **Code splitting** для оптимизации
- **Caching** стратегии

### Безопасность
- **JWT токены** для аутентификации
- **CSRF защита** для форм
- **Rate limiting** для API
- **Input validation** на всех уровнях

### Масштабируемость
- **Модульная архитектура**
- **API-first подход**
- **Микросервисная готовность**
- **Cloud-native дизайн**

---

## 📊 Метрики успеха

### Технические KPI
- Время загрузки страниц < 2 сек
- Время отклика API < 500мс
- Доступность 99.9%
- SEO score > 90

### Бизнес KPI
- Конверсия регистрации > 15%
- Завершаемость курсов > 70%
- NPS > 50
- Время в системе > 30 мин/сессия

---

## 🚀 Деплой и инфраструктура

### Локальная разработка
```bash
# Установка зависимостей
npm install

# Настройка базы данных
npx prisma generate
npx prisma db push

# Запуск в режиме разработки
npm run dev
```

### Продакшн деплой
1. **Vercel** - автоматический деплой из Git
2. **Supabase** - база данных и аутентификация
3. **Cloudinary** - хостинг медиа файлов
4. **Stripe** - обработка платежей
5. **Resend** - отправка email

---

## 📝 Следующие шаги

1. **Подтверждение плана** - обсуждение и корректировка
2. **Настройка окружения** - подготовка к разработке
3. **Создание дизайн-макетов** - UI/UX прототипы
4. **Начало разработки** - старт с MVP фазы

---

*Этот план обеспечивает поэтапную разработку профессиональной платформы онлайн-академии с возможностью масштабирования и добавления новых функций.*

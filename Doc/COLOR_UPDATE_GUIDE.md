# 🎨 Обновление цветовой схемы

## ✅ Заменены серые цвета на синие

### 🔄 Что было изменено:

#### 1. **Основные фоны**:
- `bg-gray-50` → `bg-blue-50` (светло-синий фон)
- `bg-gray-100` → `bg-blue-100` (где применимо)

#### 2. **Текст**:
- `text-gray-600` → `text-blue-600` (основной текст)
- `text-gray-900` → `text-blue-900` (заголовки)
- `text-gray-500` → `text-blue-500` (вторичный текст)

#### 3. **Интерактивные элементы**:
- `hover:bg-gray-50` → `hover:bg-blue-50` (hover эффекты)
- `border-gray-300` → `border-blue-300` (границы)
- `text-gray-400` → `text-blue-400` (иконки)

#### 4. **Обновленные файлы**:
- ✅ `src/app/layout.tsx` - основной фон
- ✅ `src/app/admin/builder/page.tsx` - конструктор курсов
- ✅ `src/app/courses/page.tsx` - страница курсов
- ✅ `src/app/dashboard/page.tsx` - дашборд
- ✅ `src/app/page.tsx` - главная страница

## 🎯 Результат:

### ✅ Преимущества новой цветовой схемы:
1. **Современный вид** - синий цвет более современный и профессиональный
2. **Лучшая читаемость** - синий текст лучше контрастирует с белым фоном
3. **Единообразие** - вся система теперь использует единую цветовую палитру
4. **Брендинг** - соответствует логотипу Neetrino Academy (синий)

### 🎨 Цветовая палитра:
- **Основной синий**: `blue-600` (#2563eb)
- **Светло-синий фон**: `blue-50` (#eff6ff)
- **Синий текст**: `blue-600` (#2563eb)
- **Темно-синий заголовки**: `blue-900` (#1e3a8a)
- **Синий hover**: `blue-50` (#eff6ff)

## 🚀 Следующие шаги:

### Для полного обновления нужно:
1. **Обновить остальные страницы** - assignments, lessons, tests
2. **Проверить компоненты** - Header, CourseCard, формы
3. **Обновить модальные окна** - шаблоны, формы
4. **Проверить адаптивность** - мобильная версия

### Рекомендации:
- Использовать `blue-600` для основных кнопок
- Использовать `blue-50` для фонов и hover эффектов
- Использовать `blue-900` для заголовков
- Использовать `blue-600` для обычного текста

---

**Цветовая схема обновлена!** 🎉

Теперь интерфейс выглядит более современно и профессионально с единой синей цветовой палитрой.

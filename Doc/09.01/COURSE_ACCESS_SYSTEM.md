# 🔒 Система контроля доступа к курсам

## 📋 Описание

Реализована комплексная система контроля доступа к курсам на основе статуса оплаты с возможностью ручного управления со стороны администраторов.

## 🎯 Основные функции

### 👤 **Для студентов:**

1. **Автоматическая блокировка доступа** при отсутствии оплаты
2. **Информативные сообщения** о статусе оплаты и причинах блокировки  
3. **Запрос временного доступа** при просрочке платежа
4. **Переход к оплате** прямо из блокированного курса

### 👨‍💼 **Для администраторов/учителей:**

1. **Ручная разблокировка доступа** (например, при задержке оплаты на 2 дня)
2. **Изменение статуса оплаты** (отметить как оплаченный/неоплаченный)
3. **Массовое управление** доступом к курсам для каждого студента
4. **Уведомления о запросах** временного доступа от студентов

---

## 🔧 Как это работает

### 🚪 Контроль доступа

Система проверяет несколько факторов:

```
✅ Есть доступ если:
   - Курс оплачен (paymentStatus = 'PAID') ИЛИ
   - Администратор вручную предоставил доступ (status = 'ACTIVE')

❌ Доступ заблокирован если:
   - Курс не оплачен (paymentStatus = 'PENDING'/'OVERDUE') И
   - Нет ручной разблокировки от админа
```

### 📊 Типы статусов

#### **Статус оплаты (PaymentStatus):**
- `PENDING` - Ожидает оплаты
- `PAID` - Оплачено  
- `OVERDUE` - Просрочено
- `CANCELLED` - Отменено

#### **Статус доступа (EnrollmentStatus):**  
- `ACTIVE` - Доступ активен (ручная разблокировка)
- `INACTIVE` - Доступ неактивен

---

## 🛠️ Управление из админки

### 📍 Где найти:
**Админ панель → Студенты → [Выбрать студента] → "Управление доступом"**

### ⚙️ Доступные действия:

1. **🔓 Предоставить доступ** - разблокировать курс независимо от оплаты
2. **🔒 Заблокировать доступ** - заблокировать даже при наличии оплаты  
3. **💳 Отметить оплаченным** - изменить статус на "Оплачено"
4. **❌ Отметить неоплаченным** - изменить статус на "Ожидает оплаты"

---

## 📱 Пользовательский интерфейс

### 🚫 Блокировка доступа:
Когда студент заходит на заблокированный курс, он видит:

- 📊 **Карточку со статусами** (оплата, доступ, следующий платеж)
- 💡 **Объяснение причины** блокировки
- 🔗 **Кнопки действий:**
  - "Перейти к оплате" → `/payments`
  - "Связаться с поддержкой" → `/contact`  
  - "Запросить временный доступ" (при просрочке)

### ✅ Разрешенный доступ:
Студент видит обычный контент курса без ограничений.

---

## 🎛️ API endpoints

### Проверка доступа:
```
GET /api/courses/[id]/access
```

### Запрос временного доступа:
```  
POST /api/courses/[id]/request-access
```

### Управление из админки:
```
PUT /api/admin/students/[id]/access
```

---

## 📝 Примеры использования

### **Сценарий 1: Задержка оплаты**
1. Студент не оплатил вовремя → доступ заблокирован
2. Студент нажимает "Запросить временный доступ"  
3. Администратор получает уведомление
4. Администратор нажимает "Предоставить доступ" → студент продолжает обучение
5. После оплаты администратор отмечает курс как "Оплаченный"

### **Сценарий 2: Наличная оплата**
1. Студент оплатил наличными администратору
2. Администратор заходит в управление доступом  
3. Нажимает "Отметить оплаченным" → доступ автоматически открывается

### **Сценарий 3: Блокировка проблемного студента**
1. Администратор заходит в управление доступом
2. Нажимает "Заблокировать доступ" → доступ закрывается даже при оплате
3. Студент видит сообщение о блокировке

---

## ✨ Преимущества системы

- 🔒 **Автоматический контроль** доступа на основе оплаты
- 🎛️ **Гибкое управление** со стороны администрации  
- 📢 **Прозрачность** для студентов (понятные сообщения о статусе)
- ⚡ **Быстрая разблокировка** в исключительных случаях
- 📊 **Полная отчетность** по статусам доступа и платежей

**Теперь у вас есть полный контроль над доступом к курсам! 🎉**
